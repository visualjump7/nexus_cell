// Default render for the @modal parallel slot when no overlay is active.
// Returning null keeps the slot empty so the landing renders alone.
export default function ModalDefault() {
  return null
}
